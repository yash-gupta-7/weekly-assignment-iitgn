Daily Assignment (Exercises)  —  Week 08 · WednesdayPG Diploma · AI-ML & Agentic AI Engineering · IIT GandhinagarWeek / DayWeek 08 · Wednesday  (Apr 15)  —  CNNs + EmbeddingsTopicsAM: CNNs — Convolution, Pooling, LeNet to ResNet, Transfer LearningPM (IIT-GN): Embeddings — Word2Vec → GloVe → BERT → Sentence-BERT, Cosine Similarity, Semantic SearchDatasetsocial_media_posts.csv  (provided on LMS)  ·  MNIST via torchvision / keras.datasetsSocial media ref: kaggle.com/datasets/cosmos98/twitter-and-reddit-sentimental-analysis-datasetMNIST ref: kaggle.com/datasets/hojjatk/mnist-datasetDuration60–90 minutes  |  Sub-steps 1–5 in-session  ·  Sub-steps 6–7 take-home optionalSubmissionPush notebook to week-08/wednesday/ · Paste link in LMSDeadlineThursday · 09:15 AM✅  Before You Begin — PreconditionsEnsure the following are ready before starting. Use the LMS checkpoint if anything is missing.📂  social_media_posts.csv loaded (3,000 rows — platform, language, topic, sentiment, hate/spam flags)📓  MNIST accessible via standard library (torchvision, keras.datasets, or sklearn)⚠️  Examine the class distribution of hate_speech and spam flags before any modeling — it matters for every evaluation decisionThe ScenarioMeera Nair, Head of Trust & Safety at a social media analytics firm, needs two things before an investor demo tomorrow. First: a content classifier that can identify potentially harmful posts. Second: a semantic search system that can surface conceptually similar posts even when users phrase them differently — critical for detecting coordinated campaigns that rotate wording to evade keyword filters.The social_media_posts.csv dataset is not clean and is not balanced. Both problems need to be understood before any model is built. The MNIST dataset provides a controlled visual benchmark for the CNN track.The seven sub-steps are structured as: clean (1–2), build (3–4), evaluate (5), alternate approach (6–7).🟢  Easy  —  Required   ·   Sub-steps 1 & 2Sub-steps 1 and 2 are about understanding and cleaning the data. Complete both before moving to Medium.1.  Load social_media_posts.csv. Before building any model, characterise the dataset fully: examine class distributions, identify data quality issues, understand what the language and engagement columns tell you. Document everything you find. What problem does the class distribution create for any classifier built on this data — and how does your documentation determine the evaluation choices you will make in Sub-step 5?2.  Load MNIST using your preferred library. Characterise the data: distribution across digit classes, image dimensions, pixel value range. What, if anything, needs to be addressed before training a CNN? Document your findings and produce a dataset that is ready for Sub-step 3.🟡  Medium  —  Required   ·   Sub-steps 3, 4 & 5Sub-steps 3 and 4 are about building the model. Sub-step 5 is your evaluation and recommendation. No method hints — the approach is yours to decide and defend.3.  Build and train a CNN on MNIST. Your architecture must have at least two convolutional layers. Every architectural decision is yours to make. Visualise the learned filters from the first convolutional layer — what has the network learned to detect? This question is not rhetorical; your answer feeds directly into Sub-step 6.4.  Using social_media_posts.csv, build a hate speech detector. The class distribution you documented in Sub-step 1 makes this non-trivial. Decide how to address it. Build a semantic similarity system using sentence embeddings that, given a known hate speech post, finds the most semantically similar posts in the dataset — including those that share no keywords with the query.5.  Combine your Sub-step 4 classifier and semantic retrieval into a two-stage moderation pipeline. Evaluate the combined system: how many additional harmful posts does Stage 2 surface that Stage 1 missed? Choose the evaluation metric that best reflects what matters to Meera's team and justify it. Present the result as a recommendation with the estimated daily review volume at 100,000 posts per day.🔴  Hard  —  Optional  (required to reach Band 4)   ·   Sub-steps 6 & 7Hard sub-steps test whether your understanding of CNNs and embeddings is deep enough to reason about failure modes and architectural tradeoffs.6.  Your Sub-step 4 semantic search system uses sentence embeddings for similarity. A colleague argues that TF-IDF cosine similarity achieves the same result without the overhead. Test this claim empirically on the social_media_posts.csv corpus using the same queries. Does TF-IDF surface the same posts? Quantify the difference and explain — with reference to what the CNN filter visualisations in Sub-step 3 revealed about learned representations — why embedding-based similarity captures something TF-IDF cannot.7.  Your Sub-step 3 CNN was trained on a well-balanced dataset (MNIST) with clean, centred images. The social_media_posts.csv dataset is imbalanced and has posts in mixed languages. Design and conduct an experiment that tests whether a CNN trained on MNIST filter representations (used as a feature extractor on image thumbnails of text, or on spectrogram-like representations of post metadata) transfers usefully to the social media classification task. Does transfer work here? Analyse why or why not — this is the question that separates engineers who understand representation learning from those who can only run model.fit().AI Usage PolicyAI tools are permitted in daily exercises. The guardrails:Every AI-assisted sub-step must include the exact prompt you used — appended to your notebook or in a prompts.md file.Include a brief critique: "Was the AI output correct? What did I change and why?"Submitting raw AI output without modification or critique zeroes your Engineering Quality score (Dimension B = 0 pts).GitHub Submission RequirementsChecked as part of Dimension C (Git / Documentation) in the weekly scoring.①week-08/ folder with a day subfolder (e.g., week-08/monday/). Not dumped in root.②README with: how to run, Python version, and packages needed.③At least 3 commits with messages describing what changed — not "final" or "done."④No .env files, API keys, __pycache__, or .ipynb_checkpoints committed.How This Exercise Is ScoredScored once per week (Saturday EOD). The TA runs your notebook end-to-end from a clean environment. "Pass" = executes and produces expected output.Total  =  Delivery + Correctness  +  Engineering Quality  +  Git / Documentation  =  /25BandDelivery +Correctness(/10)EngineeringQuality(/8)Git /Docs (/7)What it looks like410 pts8 pts7 ptsAll mandatory sub-steps pass + at least 1 Hard attempted. Modular code, readable names, defensive handling. Clear folder structure, README, 5+ meaningful commits, no junk files.38 pts6 pts5 pts70–89% pass. 3 of 4 engineering indicators visible. Reasonable structure, basic README, at least 3 commits.26 pts4 pts4 pts40–69% pass. 2 indicators. Some structure. README. At least 2 commits.13 pts2 pts2 pts<40% pass. 1 indicator. Single dump commit.00 pts0 pts0 ptsNothing submitted, broken code, no commits, or raw AI output without critique.Engineering Quality — what the TA checks for:IndicatorWhat the TA checks for① Readable namingVariable and function names describe intent — e.g., compute_adf_test(), not x or temp2.② Modular structureAt least 2 functions per sub-step. No single giant notebook cell.③ No magic numbersConstants (thresholds, window sizes) named or commented — not bare unexplained literals.④ Defensive handlingtry/except around I/O or model calls. Input shape checks. Edge cases considered.⚠️  Common mistakes that cost marksHardcoded file paths — breaks when TA runs it. Sub-step does not pass.Random train/test split on time-series or sequential data — wrong methodology. Does not pass.Justifying model choice without evidence — qualitative claims alone do not pass Medium sub-steps.Single end-of-week commit — caps Git at Band 1.All code in one flat cell — caps Engineering at Band 1.AI output without prompt or critique — Dimension B zeroed entirely.Sub-steps 6 & 7 (Hard) are optional — but at least one Hard attempt anywhere across the week is required to reach Band 4 on Delivery.
